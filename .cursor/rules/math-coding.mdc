---
description: 数学库、GIS 算法、空间索引编码规范。编辑 math/geo/algorithm/index/precision 目录文件时应用。
globs:
  - "packages/core/src/math/**"
  - "packages/core/src/geo/**"
  - "packages/core/src/algorithm/**"
  - "packages/core/src/index/**"
  - "packages/core/src/precision/**"
alwaysApply: false
---

> **提醒**：所有代码必须完整实现 + 逐行注释 + 完整类型注释 + 完整错误处理。禁止省略、骨架、TODO。详见 core.mdc。
# 数学与算法规范

## 数据类型
```typescript
export type Vec2f = Float32Array;   // length 2
export type Vec3f = Float32Array;   // length 3
export type Vec4f = Float32Array;   // length 4
export type Mat3f = Float32Array;   // length 9, column-major
export type Mat4f = Float32Array;   // length 16, column-major
export type Quatf = Float32Array;   // length 4 [x,y,z,w]
// Float64: Vec2d/Vec3d/Vec4d/Mat4d = Float64Array
// 文件：vec3.ts(f32) + vec3d.ts(f64)
```

## 函数风格
```typescript
// 必须 out 参数，零分配
export function add(out: Vec3f, a: Vec3f, b: Vec3f): Vec3f {
  out[0] = a[0] + b[0]; out[1] = a[1] + b[1]; out[2] = a[2] + b[2];
  return out;
}
```

## 规则
- `fn(out, a, b)` 签名，返回 out
- 矩阵**列主序** Column-Major（与 WGSL 对齐）
- **禁止**热路径 `new Float32Array()`，复用预分配
- 函数体短小利于 V8 JIT 内联
- CPU 坐标计算始终 Float64Array
- ECEF 坐标必须 Split-Double
- 墨卡托 zoom≥18 需 Split-Double

## 矩阵索引（列主序）
```
| m[0]  m[4]  m[8]   m[12] |
| m[1]  m[5]  m[9]   m[13] |
| m[2]  m[6]  m[10]  m[14] |
| m[3]  m[7]  m[11]  m[15] |
```

## Mat4 投影矩阵
- `perspective()` — 标准透视（WebGPU NDC Z[0,1]）
- `perspectiveReversedZ()` — Reversed-Z（近→1 远→0）
- `perspectiveReversedZInfinite()` — 地球级（far=∞）
- `ortho()` — 正交（2D 模式）
- 全部 Reversed-Z：`depthCompare:'greater'`，clear `0.0`

## WGS84 常量
```typescript
WGS84_A = 6378137.0           // 长半轴（米）
WGS84_B = 6356752.314245179   // 短半轴
WGS84_F = 1/298.257223563     // 扁率
WGS84_E2 = 0.00669437999014   // 第一偏心率²
```

## 算法清单
- earcut：带洞多边形三角剖分 O(n²)
- delaunay：点集三角剖分 O(n log n)
- convex-hull：Andrew Monotone Chain O(n log n)
- clip：Sutherland-Hodgman(矩形) / Weiler-Atherton(任意)
- intersect：11种（线段-线段/射线-三角形Möller-Trumbore/射线-椭球/射线-AABB/视锥-AABB 等）
- contain：pointInPolygon(射线法) / pointInTriangle(重心坐标)
- simplify：Douglas-Peucker / Visvalingam
- cluster：Supercluster 层级聚合 / DBSCAN / K-Means
- R-Tree：Hilbert+STR 批量加载，M=16
- Quadtree：瓦片调度用
- KD-Tree：最近邻搜索

## 测试
- 每函数 ≥3 测试（正常/边界/零值）
- 矩阵与 numpy 对比误差 <1e-6
- Vincenty 与 GeographicLib 对比 <1mm
- earcut 面积守恒

## 坐标参考系统（CRS）
- `CoordinateSystem`（L0/infra/coordinate.ts）：CRS 注册表 + 坐标转换管线
- 内置 CRS：EPSG:4326（WGS84）、EPSG:3857（Web Mercator）
- 可插拔投影模块（EP2）：用户可注册 GCJ-02、UTM、Lambert 等任意投影
- 投影数学在 `geo/projection-math.ts`：UTM/Lambert/等距圆柱/GCJ-02↔WGS84/BD-09↔GCJ-02/Helmert 七参数
- 批量坐标转换：`transformArray(input: Float64Array, fromCRS, toCRS, output?)` 高性能路径
