---
description: GeoForge 引擎全局规则——架构、依赖方向、14 条硬约束、帧循环、数据流。每次对话必须加载。
alwaysApply: true
---

# GeoForge 核心规则

## 项目
GeoForge：统一 2D/2.5D/3D 纯 WebGPU GIS 引擎。零 npm 依赖，所有算法自研。

## ⚠️ AI 代码输出规范（最高优先级）

**禁止输出简约版、骨架版、示意版代码。** 每次生成代码必须是**可直接运行的完整实现**。

1. **完整实现，不省略**——禁止 `// ...其他方法类似`、`// TODO`、`// 省略`、`/* 实现略 */`。每个函数体、每个分支、每个循环都要写完
2. **逐行注释**——每个函数必须有 JSDoc（描述、@param、@returns、@example）；函数体内关键逻辑每 3~5 行一条行内注释解释"为什么这么做"
3. **完整类型定义**——所有 interface/type/enum 的每个字段都必须有 `/** 注释 */`，说明用途、单位、取值范围、默认值
4. **完整 import/export**——文件头部列出所有 import，文件末尾或声明处使用命名 export，不省略
5. **完整错误处理**——不写 happy path only。每个可能失败的操作（GPU 调用、Worker 通信、网络请求、数组越界）都要有 try-catch 或条件检查，并写明错误处理逻辑
6. **完整边界处理**——空数组、null/undefined、NaN、Infinity、负数、零除等边界条件必须处理并注释
7. **完整常量定义**——魔法数字必须抽为命名常量，并注释其含义和来源
8. **不截断长文件**——如果一个模块需要 500 行，就输出 500 行。禁止"由于篇幅限制只展示核心部分"

<example>
// ✅ 正确：完整实现 + 完整注释
/**
 * 计算两个 3D 向量的叉积（Cross Product）。
 * 结果向量垂直于 a 和 b 所在平面，方向遵循右手定则。
 *
 * @param out - 输出向量（预分配，避免 GC）
 * @param a - 左操作数
 * @param b - 右操作数
 * @returns out 引用，便于链式调用
 *
 * @example
 * const result = vec3.create();
 * vec3.cross(result, [1,0,0], [0,1,0]); // → [0,0,1]
 */
export function cross(out: Vec3f, a: Vec3f, b: Vec3f): Vec3f {
  // 缓存分量到局部变量，避免数组多次寻址（V8 优化友好）
  const ax = a[0], ay = a[1], az = a[2];
  const bx = b[0], by = b[1], bz = b[2];

  // 叉积公式：(ay*bz - az*by, az*bx - ax*bz, ax*by - ay*bx)
  out[0] = ay * bz - az * by;
  out[1] = az * bx - ax * bz;
  out[2] = ax * by - ay * bx;

  return out;
}
</example>

<example type="invalid">
// ❌ 错误：简约版，无注释，省略实现
export function cross(out: Vec3f, a: Vec3f, b: Vec3f): Vec3f {
  // 标准叉积实现
  out[0] = a[1]*b[2] - a[2]*b[1];
  out[1] = a[2]*b[0] - a[0]*b[2];
  out[2] = a[0]*b[1] - a[1]*b[0];
  return out;
}
</example>

## 约束
1. 一个引擎所有维度
2. Tree-Shakable（2D ~120KB / 3D ~195KB / 全功能 ~350KB gz）
3. 同场景多维度共存（多投影 Render Pass + Compositor）
4. 纯 WebGPU，无 WebGL
5. 6 个扩展点对标 Three.js 生态

## 七层架构
```
L6 预设层  Map2D / Map25D / Globe3D / MapFull
L5 扩展层  ExtensionRegistry + Lifecycle + EP1~EP6
L4 场景层  SceneGraph / LayerManager / SourceManager / StyleEngine / LabelManager / GlyphManager / FeatureStateManager / AntiMeridianHandler / AnimationManager / SpatialQuery / A11yManager
L3 调度层  FrameScheduler / TileScheduler / WorkerPool / ResourceManager / MemoryBudget / RequestScheduler / ErrorRecovery / CameraController(2D/25D/3D) / ViewMorph
L2 渲染层  ShaderAssembler / PipelineCache / DepthManager / RenderGraph / FrameGraphBuilder / Compositor / PickingEngine / StencilManager / RenderStats / ComputePassManager / BlendPresets / UniformLayoutBuilder / WGSLTemplates
L1 GPU层   DeviceManager / BufferPool / TextureManager / GPUMemoryTracker / IndirectDrawManager / SurfaceManager / BindGroupCache / GPUUploader
L0 基础层  math/ geo/ algorithm/ index/ precision/ types/ infra/
```
依赖：L6→L5→L4→L3→L2→L1→L0，禁止反向或跨层。

## 14 条硬约束
1. **先查架构再写代码**——确认模块在七层中的位置
2. **类型从 L0 import**——Feature/CameraState/PickResult/Viewport/BBox2D/TileCoord/StyleExpression 禁止重定义
3. **零依赖**——禁止任何 npm 包
4. **out 参数**——数学函数 `fn(out, a, b)`
5. **GPU 走 L1**——L2~L6 禁止 `device.createBuffer/createTexture`
6. **Shader 走 Assembler**——禁止单体 WGSL，拆投影/几何/样式模块
7. **Worker 走 Pool**——CPU 密集计算走 `WorkerPool.submit()`
8. **扩展走 Context**——EP1~EP6 禁止 import 内部模块
9. **错误隔离**——扩展 `safeExecute` 包裹
10. **Reversed-Z**——`depthCompare:'greater'`，clear `0.0`，格式 `depth32float`
11. **列主序**——矩阵 Column-Major 对齐 WGSL
12. **bearing 非 heading**——旋转角统一 `bearing`（弧度）
13. **异步 Picking**——`queryRenderedFeatures` 返回 `Promise<Feature[]>`
14. **Tree-Shaking**——命名 export，无 default，无顶层副作用

## 帧循环
```
UPDATE → Camera.update / Animation.update / TileScheduler.update / LabelManager粗筛
RENDER → FrameGraphBuilder → Compute(cull/sort/collision) → Render(按投影分组) → Composite → PostProcess → Screen → Picking → submit
POST   → Stats / MemoryBudget / StagingRing
IDLE   → ResourceManager.idle / PipelineCache.warmup
```

## 数据流 addLayer→像素
```
L6 addLayer → L4 LayerManager(工厂→StyleEngine.compile→ShaderAssembler.assemble→PipelineCache)
→ L3 TileScheduler→RequestScheduler→fetch(MVT)→WorkerPool('mvt-decode': earcut+宽线→transferable)
→ L1 Uploader.uploadFromTransferable→GPU Buffer
→ L2 FrameGraphBuilder→layer.encode(setPipeline/setBuffer/setBindGroup/drawIndexed) → GPU 像素
```

## 初始化链
L0 → L1(Device→Surface→MemTracker→BufferPool→Texture→BindGroup→IndirectDraw→Uploader) → L2(Blend→Depth→Stencil→UniformLayout→ShaderAssembler→PipelineCache→ComputePass→Stats→Compositor→Picking→RenderGraph) → L3(ErrorRecovery→Request→Worker→Resource→Memory→Tile→Camera→FrameScheduler) → L4(FeatureState→Glyph→Style→Label→Source→Animation→Query→A11y→Layer→Scene) → L5(Registry→Lifecycle→Interaction→内置投影/交互) → L6(样式→视图→Controls→start)

## 设计决策
Shader=模块组合+Variant缓存 | CPU-GPU=流水线并行(N+1/N) | 混合=多Pass+Compositor | 精度=SplitDouble+RTC | 深度=ReversedZ+对数 | 瓦片=SSE LOD | 碰撞=CPU R-Tree+GPU Compute | 内存=引用计数+LRU+双轨预算 | 文字=MSDF | 线=SDF | 宽线=Worker条带 | OIT=WeightedBlended | 阴影=CSM+PCF

## 错误恢复
瓦片=指数退避(base×2^n,max30s,jitter) | Worker崩溃=重启+重提交 | GPU丢失=销毁→重建Device→重建资源 | 扩展=safeExecute→5次禁用

## 内存淘汰
引用计数=0 → LRU → 不可见瓦片 → 远处瓦片 → 类型(纹理>Buffer>Pipeline)

## 可选分析包
`@geoforge/analysis`（9 子模块）：boolean（交/并/差）/ buffer（缓冲区）/ interpolation（TIN/IDW/等值线）/ classification（Jenks/分位数）/ grid（Hex/Voronoi）/ raster（坡度/山影/视域）/ transform / aggregation / topology

## 移动端
`compat-mobile` 包：能力检测→降质策略→激进 LOD→`powerPreference:'high-performance'`→降渲染分辨率→减后处理 Pass

## Git
`type(scope): description` — feat/fix/refactor/perf/test/docs/chore
